import BaseService from "@/plugins/services/BaseService";

const RESOURCE_NAME = "/parametro";

export default class ParametroService extends BaseService {
  constructor() {
    super(RESOURCE_NAME);
  }
}
