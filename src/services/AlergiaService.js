import BaseService from "@/plugins/services/BaseService";

const RESOURCE_NAME = "/alergia";

export default class AlergiaService extends BaseService {
  constructor() {
    super(RESOURCE_NAME);
  }
}
