import BaseService from "@/plugins/services/BaseService";

const RESOURCE_NAME = "/resultadoExames";

export default class ResultadoExames extends BaseService {
  constructor() {
    super(RESOURCE_NAME);
  }
}
